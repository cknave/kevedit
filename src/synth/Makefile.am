# -*-Makefile-*-, or close enough

#AUTOMAKE_OPTIONS =	1.6.3 foreign

noinst_LIBRARIES = libsynth.a

libsynth_a_SOURCES = notes.c notes.h pcspeaker.c pcspeaker.h synth.h zzm.c zzm.h

EXTRA_DIST = $(OPTIONAL_SRC) play.c
