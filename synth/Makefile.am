# -*-Makefile-*-, or close enough

#AUTOMAKE_OPTIONS =	1.6 foreign

# TODO: pcspeaker support

noinst_LIBRARIES = libsynth.a

libsynth_a_SOURCES = notes.c notes.h sdl_synth.c sdl_synth.h synth.h zzm.c zzm.h

AM_CFLAGS =	`sdl-config --cflags`
